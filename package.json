{
  "name": "ghl-rabbitsign-worker",
  "version": "1.0.0",
  "description": "Cloudflare worker that acts as a middleman between an Express server and different software. Current capabilities, E-sign and GHL CRM.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "jest --runInBand",
    "lint": "echo \"(add ESLint later)\""
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "type": "module"
}
